runtime: python3.11
memory_size: 256
timeout: 15
handler: lambda_function.lambda_handler
description: "Handle WebSocket messages (ping/pong, etc)"
environment_variables:
  AWS_REGION: "us-east-1"
  DYNAMODB_CONNECTIONS_TABLE: "websocket_connections"
